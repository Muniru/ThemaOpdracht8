---
title: "Eindopdracht"
author: "Muniru & Sam"
date: '2022-06-03'
output:
  pdf_document:
    toc: true
    toc_depth: 2
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
library(deSolve)
library(pracma)
r <- 0.11
rho <- 1.29
c <- 0.5

parameters <- c(m = 0.445, k = (1/2) * c * rho * (pi * r^2),
                mu = 0.1, g = c(x = 0, y = -9.81))

state_straight <- c(velocity = c(x = 10, y = 5),
                    position = c(x = 0, y = 0))

times <- seq(0, 10, 1)

model <- function(t, y, parms){
  with(as.list(c(y, parms)), {
    g <- c(g.x, g.y)
    velocity <- c(velocity.x, velocity.y)
    position <- c(position.x, position.y)
    
    acc <-  (m * g - k * velocity * sqrt(velocity.x^2 + velocity.y^2)) / m 
    
    
    print(c(t, "time"))    
    print(c(velocity, "velocity"))
    print(c(acc, "acceleration"))
    print("")

    
    return(list(c(acc, velocity)))
  }
  )
}

out <- as.data.frame(ode(times = times, y = state_straight, parms = parameters, func = model, method = "euler"))

plot(out$position.y, type = "l")

```


