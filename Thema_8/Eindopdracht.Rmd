---
title: "Eindopdracht"
author: "Muniru & Sam"
date: '2022-06-03'
output:
  pdf_document:
    toc: true
    toc_depth: 2
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
parameters <- c(m = 0.445, r = 0.11,
                rho = 1.29, c = 0.5,
                mu = 0.1, g = 9.81)

state.straight <- c(omega = c(i = 0, k = 0, j = 0),
                    velocity = c(x = 5, y = 28, z = 11))

state.curve <- c(omega = c(i = 2, k = -2, j = 16),
                 velocity = c(x = 5, y = 28, z = 11))

state.up <- c(omega = c(i = 13, k = 0, j = 0),
              velocity = c(x = 5, y = 28, z = 11))

start.position <- c(x = 7.23, y = 0, z = 0)

times <- seq(0,
             100,
             by = 1)


# The model we will use
model <- function(t, y, parms){
  with(as.list(c(y, parms)),{
    
    return(list(c(f)))
    })
}

out  <- as.data.frame(ode(times = times,
            y = state.curve,
            parms = parameters,
            func = model, 
            method = "euler"))

plot(out)

```


